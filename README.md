# Yefai Project

This project contains a Next.js frontend in the `client` directory and a FastAPI backend in the `server` directory.

## Client
See `client/README.md`

## Server
See `server/README.md`